/*find are of rectangle*/


#include<stdio.h>
int main()
{
    int length,width,area;
    printf("enter the value of length:");
    scanf("%d",&length);
    printf("enter the value of width:");
    scanf("%d",&width);
    area=length*width;/*formula of area f */
    printf("the area of rectangle:%d",area);
    return 0;
}
